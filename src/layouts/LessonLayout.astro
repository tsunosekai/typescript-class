---
import BaseLayout from './BaseLayout.astro';

interface Props {
  title: string;
  lesson: number;
  goal?: string;
  time?: string;
  checklist?: string[];
}

const { title, lesson, goal, time, checklist } = Astro.props;
const prevLesson = lesson > 1 ? String(lesson - 1).padStart(2, '0') : null;
const nextLesson = lesson < 23 ? String(lesson + 1).padStart(2, '0') : null;
const lessonPadded = String(lesson).padStart(2, '0');
---

<BaseLayout title={`第${lesson}回: ${title}`}>
  <article>
    <div class="lesson-header">
      <span class="lesson-number">第{lesson}回</span>
      <h1>{title}</h1>
    </div>

    {(goal || time || checklist) && (
      <div class="lesson-meta">
        {time && <div class="lesson-time">⏱ {time}</div>}
        {goal && <div class="lesson-goal">🎯 ゴール: {goal}</div>}
        {checklist && checklist.length > 0 && (
          <details class="lesson-checklist">
            <summary>📋 最低限わかればOKチェックリスト</summary>
            <ul>
              {checklist.map((item) => <li>{item}</li>)}
            </ul>
          </details>
        )}
      </div>
    )}

    <div class="lesson-content">
      <slot />
    </div>

    <nav class="lesson-nav">
      {prevLesson && (
        <a href={`/lessons/${prevLesson}`} class="nav-prev">
          ← 前の回へ
        </a>
      )}
      <a href="/" class="nav-home">目次に戻る</a>
      {nextLesson && (
        <a href={`/lessons/${nextLesson}`} class="nav-next">
          次の回へ →
        </a>
      )}
    </nav>
  </article>
</BaseLayout>

<style>
  .lesson-header {
    margin-bottom: 2rem;
  }

  .lesson-number {
    display: inline-block;
    background-color: #2563eb;
    color: white;
    padding: 0.25rem 0.75rem;
    border-radius: 9999px;
    font-size: 0.875rem;
    font-weight: bold;
    margin-bottom: 0.5rem;
  }

  .lesson-header h1 {
    margin-top: 0.5rem;
    border-bottom: none;
  }

  .lesson-meta {
    background-color: #f0f9ff;
    border: 1px solid #bae6fd;
    border-radius: 8px;
    padding: 1rem 1.25rem;
    margin-bottom: 2rem;
  }

  .lesson-time {
    font-size: 0.9rem;
    color: #64748b;
    margin-bottom: 0.5rem;
  }

  .lesson-goal {
    font-weight: 600;
    color: #0369a1;
    margin-bottom: 0.75rem;
  }

  .lesson-checklist {
    margin-top: 0.5rem;
  }

  .lesson-checklist summary {
    cursor: pointer;
    font-size: 0.9rem;
    color: #475569;
  }

  .lesson-checklist ul {
    margin: 0.5rem 0 0 0;
    padding-left: 1.5rem;
  }

  .lesson-checklist li {
    font-size: 0.9rem;
    color: #475569;
    margin-bottom: 0.25rem;
  }

  .lesson-nav {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 1rem;
    margin-top: 3rem;
    padding-top: 2rem;
    border-top: 1px solid #e2e8f0;
  }

  .lesson-nav a {
    padding: 0.75rem 1.5rem;
    background-color: #f1f5f9;
    border-radius: 8px;
    text-decoration: none;
    color: #334155;
    font-weight: 500;
    transition: background-color 0.2s;
  }

  .lesson-nav a:hover {
    background-color: #e2e8f0;
  }

  .nav-home {
    background-color: #2563eb !important;
    color: white !important;
  }

  .nav-home:hover {
    background-color: #1d4ed8 !important;
  }

  .screenshot-request {
    display: block;
    margin-top: 0.5rem;
    margin-bottom: 1rem;
    padding: 0.5rem 1rem;
    background-color: #fef3c7;
    border: 1px dashed #f59e0b;
    border-radius: 6px;
    font-size: 0.85rem;
    color: #92400e;
    text-decoration: none;
    text-align: center;
  }

  .screenshot-request:hover {
    background-color: #fde68a;
  }

  .feedback-link-wrapper {
    display: flex;
    justify-content: flex-end;
    margin-bottom: 1rem;
  }

  .feedback-link {
    padding: 0.2rem 0.5rem;
    font-size: 0.7rem;
    color: #94a3b8;
    text-decoration: none;
  }

  .feedback-link:hover {
    color: #64748b;
    text-decoration: underline;
  }
</style>

<script define:vars={{ lesson, lessonPadded }}>
  // プレースホルダー画像の下にスクショ提供リンクを追加
  document.querySelectorAll('.lesson-content img').forEach((img) => {
    const src = img.getAttribute('src') || '';
    if (src.includes('.svg')) {
      const filename = src.split('/').pop()?.replace('.svg', '') || '';
      const issueTitle = encodeURIComponent(`[スクショ] 第${lesson}回 - ${filename}`);
      const issueBody = encodeURIComponent(`## 提供するスクリーンショット

**ファイル名:** ${filename}

**レッスン番号:** 第${lesson}回

**スクリーンショット:**
<!-- 画像をドラッグ＆ドロップで貼り付けてください -->


## 備考

---
※ AIで処理するので丁寧に書かなくて大丈夫です。意味が伝われば OK！
`);
      const link = document.createElement('a');
      link.href = `https://github.com/tsunosekai/typescript-class/issues/new?title=${issueTitle}&body=${issueBody}&labels=screenshot`;
      link.className = 'screenshot-request';
      link.target = '_blank';
      link.textContent = `📸 この画像を提供する (${filename})`;
      img.parentNode?.insertBefore(link, img.nextSibling);
    }
  });

  // 各セクションの末尾（hrの上）にフィードバックリンクを追加
  document.querySelectorAll('.lesson-content hr').forEach((hr) => {
    // hrの直前の見出しを探す
    let prev = hr.previousElementSibling;
    let sectionTitle = '';
    while (prev) {
      if (prev.tagName === 'H2' || prev.tagName === 'H3') {
        sectionTitle = prev.textContent || '';
        break;
      }
      prev = prev.previousElementSibling;
    }
    if (!sectionTitle) return;

    const issueTitle = encodeURIComponent(`[フィードバック] 第${lesson}回 - ${sectionTitle}`);
    const issueBody = encodeURIComponent(`## 該当箇所

**レッスン番号:** 第${lesson}回

**セクション:** ${sectionTitle}

## 内容

### どこがわかりにくい/間違っている？
<!-- 具体的に教えてください -->


### どうすればわかりやすくなる？（任意）
<!-- 改善案があれば -->

---
※ AIで処理するので丁寧に書かなくて大丈夫です。意味が伝われば OK！
`);
    const wrapper = document.createElement('div');
    wrapper.className = 'feedback-link-wrapper';
    const link = document.createElement('a');
    link.href = `https://github.com/tsunosekai/typescript-class/issues/new?title=${issueTitle}&body=${issueBody}&labels=feedback`;
    link.className = 'feedback-link';
    link.target = '_blank';
    link.textContent = '分かりにくかった部分を報告';
    wrapper.appendChild(link);

    hr.parentNode?.insertBefore(wrapper, hr);
  });
</script>

<script>
  import mermaid from 'mermaid';

  // ライトボックス機能
  const lightbox = document.createElement('div');
  lightbox.className = 'lightbox';
  lightbox.innerHTML = `
    <span class="lightbox-close">&times;</span>
    <div class="lightbox-content"></div>
    <span class="lightbox-hint">クリックで閉じる</span>
  `;
  document.body.appendChild(lightbox);

  const lightboxContent = lightbox.querySelector('.lightbox-content')!;

  function openLightbox(content: string) {
    lightboxContent.innerHTML = content;
    lightbox.classList.add('active');
    document.body.style.overflow = 'hidden';
  }

  function closeLightbox() {
    lightbox.classList.remove('active');
    document.body.style.overflow = '';
  }

  lightbox.addEventListener('click', closeLightbox);
  lightboxContent.addEventListener('click', (e) => e.stopPropagation());

  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') closeLightbox();
  });

  // 画像クリックで拡大
  document.querySelectorAll('.lesson-content img').forEach((img) => {
    img.addEventListener('click', () => {
      openLightbox(`<img src="${img.getAttribute('src')}" alt="${img.getAttribute('alt') || ''}">`);
    });
  });

  // Mermaid初期化・レンダリング
  async function initMermaid() {
    mermaid.initialize({
      startOnLoad: false,
      theme: 'default',
      securityLevel: 'loose',
    });

    // data-language="mermaid" を持つ pre 要素を探してレンダリング
    document.querySelectorAll('pre[data-language="mermaid"]').forEach((pre) => {
      const code = pre.querySelector('code');
      if (code) {
        const text = code.textContent || '';
        const container = document.createElement('div');
        container.className = 'mermaid';
        container.textContent = text;
        pre.replaceWith(container);
      }
    });

    await mermaid.run();

    // Mermaidレンダリング後にクリックイベントを設定
    document.querySelectorAll('.mermaid').forEach((el) => {
      el.addEventListener('click', () => {
        openLightbox(el.innerHTML);
      });
    });
  }

  initMermaid();

  // 外部リンクを別タブで開く
  document.querySelectorAll('.lesson-content a').forEach((link) => {
    const href = link.getAttribute('href');
    if (href && (href.startsWith('http://') || href.startsWith('https://'))) {
      link.setAttribute('target', '_blank');
      link.setAttribute('rel', 'noopener noreferrer');
    }
  });
</script>

<style is:global>
  .mermaid {
    text-align: center;
    margin: 1.5rem 0;
    cursor: pointer;
  }

  .lesson-content img {
    cursor: pointer;
  }

  /* ライトボックス */
  .lightbox {
    display: none;
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0, 0, 0, 0.9);
    z-index: 1000;
    justify-content: center;
    align-items: center;
    cursor: pointer;
  }

  .lightbox.active {
    display: flex;
  }

  .lightbox-content {
    max-width: 95vw;
    max-height: 90vh;
    min-width: 80vw;
    overflow: auto;
    background-color: white;
    border-radius: 8px;
    padding: 2rem;
  }

  .lightbox-content img {
    width: 100%;
    height: auto;
  }

  .lightbox-content svg {
    width: 100%;
    height: auto;
    min-height: 60vh;
  }

  .lightbox-close {
    position: fixed;
    top: 1rem;
    right: 1rem;
    color: white;
    font-size: 2rem;
    cursor: pointer;
    z-index: 1001;
  }

  .lightbox-hint {
    position: absolute;
    bottom: 1rem;
    left: 50%;
    transform: translateX(-50%);
    color: rgba(255, 255, 255, 0.7);
    font-size: 0.875rem;
  }
</style>
